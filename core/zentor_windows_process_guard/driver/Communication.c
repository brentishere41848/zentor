#include "ZentorProcessGuard.h"

/*
 * Reserved for a signed-driver cache/communication implementation. The v1
 * process callback driver must not fake blocking before the service verdict
 * cache is proven under Driver Verifier.
 */
