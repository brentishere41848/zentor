name: pasus_protocol
description: Shared typed protocol models for the Pasus client.
version: 0.1.0
publish_to: none

environment:
  sdk: ^3.12.0

dependencies:
  meta: ^1.16.0

dev_dependencies:
  lints: ^6.0.0
  test: ^1.26.0
