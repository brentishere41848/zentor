# Pasus Client

Real Flutter 3 Material 3 Pasus client application.

```powershell
flutter pub get
flutter run -d windows
flutter test
```

The app uses Flutter widgets only. It does not embed a web dashboard or fake runtime data.
